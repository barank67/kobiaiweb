---
import { navItems, socials } from "../data/site";
import "../styles/global.css";

const {
  title = "Kobi AI",
  description = "Şirketler için agentic AI, yapay zeka ajanları ve iş süreci otomasyonu çözümleri.",
  image = "/assets/images/agentic-ai-bg.webp",
  type = "website",
  publishedTime,
  canonical
} = Astro.props;

const canonicalUrl = canonical ?? new URL(Astro.url.pathname, Astro.site ?? Astro.url.origin).toString();
const imageUrl = new URL(image, Astro.site ?? Astro.url.origin).toString();
---
<!doctype html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />
    <meta property="og:type" content={type} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={imageUrl} />
    {publishedTime && <meta property="article:published_time" content={publishedTime} />}
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={imageUrl} />
    <title>{title}</title>
    <link rel="icon" href="/favicon.png" />
  </head>
  <body>
    <header class="site-header">
      <a class="brand" href="/" aria-label="Kobi AI ana sayfa">
        <img class="brand-logo" src="/assets/images/kobiai-logo-white.png" alt="Kobi AI" />
      </a>
      <nav class="main-nav" aria-label="Ana menü">
        {navItems.map((item) => (
          item.children ? (
            <div class="nav-item">
              <a href={item.href} aria-haspopup="true">{item.label}</a>
              <div class="nav-menu">
                {item.children.map((child) => <a href={child.href}>{child.label}</a>)}
              </div>
            </div>
          ) : (
            <a href={item.href}>{item.label}</a>
          )
        ))}
      </nav>
      <a class="header-cta" href="/#iletisim">Demo Planla</a>
    </header>
    <main>
      <slot />
    </main>
    <section class="contact-section" id="iletisim">
      <div class="contact-copy">
        <p class="eyebrow">Demo Planla</p>
        <h2>Ekibiniz için doğru yapay zeka ajanlarını birlikte belirleyelim</h2>
        <p>Formu doldurduğunuzda bilgileriniz e-posta taslağı olarak hazırlanır. Statik GitHub Pages yayını kullandığımız için ayrı bir form servisi bağlanmadan sunucuya kayıt yapılmaz.</p>
      </div>
      <form class="contact-form" id="demo-form">
        <label>
          <span>Ad Soyad *</span>
          <input name="name" type="text" autocomplete="name" required />
        </label>
        <label>
          <span>Firma</span>
          <input name="company" type="text" autocomplete="organization" />
        </label>
        <label>
          <span>Telefon *</span>
          <input name="phone" type="tel" autocomplete="tel" required />
        </label>
        <label>
          <span>Email *</span>
          <input name="email" type="email" autocomplete="email" required />
        </label>
        <label class="full-field">
          <span>Mesajınız</span>
          <textarea name="message" rows="5"></textarea>
        </label>
        <button class="button primary" type="submit">Demo Talebi Gönder</button>
        <p class="form-status" role="status" aria-live="polite"></p>
      </form>
    </section>
    <footer class="site-footer">
      <div>
        <a class="brand footer-brand" href="/">
          <img class="brand-logo" src="/assets/images/kobiai-logo-white.png" alt="Kobi AI" />
        </a>
        <p>Yapay zeka ajanlarıyla KOBİ operasyonlarını daha hızlı, ölçülebilir ve verimli hale getiren çözüm ortağı.</p>
      </div>
      <div>
        <h2>İletişim</h2>
        <a href="mailto:info@kobiai.com.tr">info@kobiai.com.tr</a>
        <a href="tel:+905333346246">+90 533 334 62 46</a>
      </div>
      <div>
        <h2>Sosyal Medya</h2>
        <div class="social-links">
          {socials.map((item) => (
            <a href={item.href} target="_blank" rel="noreferrer" aria-label={item.label}>
              <img src={item.icon} alt="" />
              <span>{item.label}</span>
            </a>
          ))}
        </div>
      </div>
    </footer>
    <button class="chatbot-toggle" type="button" aria-label="Chatbot'u aç" aria-expanded="false">
      <img src="/assets/images/kobi-ai-chatbot-icon.png" alt="" />
    </button>
    <section class="chatbot-panel" aria-live="polite" aria-hidden="true">
      <div class="chatbot-head">
        <strong>Kobi AI Asistan</strong>
        <button type="button" aria-label="Chatbot'u kapat">×</button>
      </div>
      <div class="chatbot-body">
        <div class="chatbot-messages">
          <p class="bot-message">Merhaba, yapay zeka ajanları hakkında nasıl yardımcı olabilirim?</p>
        </div>
        <div class="chatbot-actions">
          <button type="button" data-chat-prompt="Demo planlamak istiyorum.">Demo talebi oluştur</button>
          <button type="button" data-chat-prompt="Şirketim için çözüm önerisi almak istiyorum.">Çözüm önerisi al</button>
        </div>
        <form class="chatbot-form">
          <input type="text" name="chat-message" autocomplete="off" placeholder="Mesajınızı yazın" aria-label="Chatbot mesajı" />
          <button type="submit">Gönder</button>
        </form>
      </div>
    </section>
    <script is:inline>
      document.addEventListener("DOMContentLoaded", () => {
        const toggle = document.querySelector(".chatbot-toggle");
        const panel = document.querySelector(".chatbot-panel");
        const close = document.querySelector(".chatbot-head button");
        const messages = document.querySelector(".chatbot-messages");
        const chatForm = document.querySelector(".chatbot-form");
        const chatInput = document.querySelector(".chatbot-form input");
        const demoForm = document.querySelector("#demo-form");
        const formStatus = document.querySelector(".form-status");
        const setOpen = (open) => {
          panel?.classList.toggle("is-open", open);
          panel?.setAttribute("aria-hidden", open ? "false" : "true");
          toggle?.setAttribute("aria-expanded", open ? "true" : "false");
          if (open) window.setTimeout(() => chatInput?.focus(), 120);
        };
        const addMessage = (text, type = "bot") => {
          const item = document.createElement("p");
          item.className = type === "user" ? "user-message" : "bot-message";
          item.textContent = text;
          messages?.appendChild(item);
          messages?.scrollTo({ top: messages.scrollHeight, behavior: "smooth" });
        };
        const replyTo = (text) => {
          const lower = text.toLocaleLowerCase("tr-TR");
          if (lower.includes("demo") || lower.includes("görüş")) {
            addMessage("Demo talebi için form bölümüne yönlendirebilirim. Ad, telefon ve e-posta alanlarını doldurmanız yeterli.");
            return;
          }
          if (lower.includes("satın") || lower.includes("finans") || lower.includes("üretim") || lower.includes("teklif")) {
            addMessage("Bu süreç için uygun ajanı belirlemek adına mevcut veri kaynaklarınızı ve tekrar eden karar noktalarını birlikte inceleyebiliriz.");
            return;
          }
          addMessage("Mesajınızı aldım. Daha detaylı dönüş için Demo Planla formunu doldurabilir veya info@kobiai.com.tr adresine yazabilirsiniz.");
        };
        toggle?.addEventListener("click", () => {
          setOpen(!panel?.classList.contains("is-open"));
        });
        close?.addEventListener("click", () => setOpen(false));
        document.querySelectorAll("[data-chat-prompt]").forEach((button) => {
          button.addEventListener("click", () => {
            const text = button.getAttribute("data-chat-prompt") || "";
            addMessage(text, "user");
            replyTo(text);
            if (text.includes("Demo")) document.querySelector("#iletisim")?.scrollIntoView({ behavior: "smooth" });
          });
        });
        chatForm?.addEventListener("submit", (event) => {
          event.preventDefault();
          const text = chatInput?.value.trim();
          if (!text) return;
          addMessage(text, "user");
          chatInput.value = "";
          replyTo(text);
        });
        demoForm?.addEventListener("submit", (event) => {
          event.preventDefault();
          if (!demoForm.reportValidity()) return;
          const data = new FormData(demoForm);
          const body = [
            `Ad Soyad: ${data.get("name")}`,
            `Firma: ${data.get("company") || "-"}`,
            `Telefon: ${data.get("phone")}`,
            `Email: ${data.get("email")}`,
            "",
            `Mesaj: ${data.get("message") || "-"}`
          ].join("\n");
          window.location.href = `mailto:info@kobiai.com.tr?subject=${encodeURIComponent("Kobi AI Demo Talebi")}&body=${encodeURIComponent(body)}`;
          formStatus.textContent = "E-posta taslağı hazırlandı. Gönderimi e-posta uygulamanızdan tamamlayabilirsiniz.";
        });
        document.addEventListener("keydown", (event) => {
          if (event.key === "Escape") setOpen(false);
        });
      });
    </script>
  </body>
</html>
